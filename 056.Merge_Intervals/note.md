* Arrays.sort(intervals,(i1,i2) -> Integer.compare(i1[0],i2[0]))
* List.toArray(T[] a): Returns an array containing all of the elements in this list in proper sequence (from first to last element); the runtime type of the returned array is that of the specified array.
